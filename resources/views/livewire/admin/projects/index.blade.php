<div>
    <div class="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p class="font-mono text-xs uppercase tracking-[0.2em] text-blue-400">Contenu</p><h1 class="mt-2 text-3xl font-semibold text-white">Projets</h1></div><a href="{{ route('backoffice.projects.create') }}" class="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-500">Nouveau projet</a></div>
    @if (session('success'))<div class="mb-5 rounded-lg border border-emerald-500/30 bg-emerald-500/10 px-4 py-3 text-sm text-emerald-300">{{ session('success') }}</div>@endif
    <div class="mb-4"><input wire:model.live.debounce.300ms="search" type="search" placeholder="Rechercher un projet..." class="w-full rounded-lg border border-slate-700 bg-slate-900 px-4 py-3 text-sm text-white placeholder:text-slate-500 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500 sm:max-w-md"></div>
    <div class="overflow-hidden rounded-xl border border-slate-800 bg-slate-900"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b border-slate-800 text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-5 py-4">Projet</th><th class="px-5 py-4">Stack</th><th class="px-5 py-4">Statut</th><th class="px-5 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-800">@forelse ($projects as $project)<tr class="transition hover:bg-slate-800/50"><td class="px-5 py-4"><div class="flex items-center gap-3">@if ($project->image)<img src="{{ $project->image }}" alt="" class="h-10 w-10 rounded-lg object-cover">@else<div class="h-10 w-10 rounded-lg bg-slate-800"></div>@endif<div><p class="font-medium text-white">{{ $project->name }}</p><p class="font-mono text-xs text-slate-500">{{ $project->slug }}</p></div></div></td><td class="px-5 py-4 text-slate-400">{{ $project->stack?->name ?? '—' }}</td><td class="px-5 py-4"><span class="rounded-full bg-blue-500/10 px-2.5 py-1 text-xs text-blue-300">{{ $project->status?->label() }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('backoffice.projects.edit', $project) }}" class="text-blue-400 hover:text-blue-300">Modifier</a><button wire:click="delete({{ $project->id }})" wire:confirm="Supprimer ce projet ?" class="ml-4 text-red-400 hover:text-red-300">Supprimer</button></td></tr>@empty<tr><td colspan="4" class="px-5 py-12 text-center text-slate-400">Aucun projet trouvé.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-800 px-5 py-4">{{ $projects->links() }}</div></div>
</div>
