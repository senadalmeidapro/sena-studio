<div>
    <div class="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p class="font-mono text-xs uppercase tracking-[0.2em] text-blue-400">Administration</p><h1 class="mt-2 text-3xl font-semibold tracking-tight text-white">{{ $definition['title'] }}</h1></div><span class="font-mono text-xs text-slate-500">{{ str_pad((string) $records->total(), 2, '0', STR_PAD_LEFT) }} éléments</span></div>
    @if (session('success'))<div class="mb-5 rounded-lg border border-emerald-500/30 bg-emerald-500/10 px-4 py-3 text-sm text-emerald-300">{{ session('success') }}</div>@endif
    <div class="mb-4 flex items-center gap-3"><input wire:model.live.debounce.300ms="search" type="search" placeholder="Rechercher..." class="w-full max-w-md rounded-lg border border-slate-700 bg-slate-900 px-4 py-3 text-sm text-white placeholder:text-slate-500 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"><span class="hidden font-mono text-xs text-slate-600 sm:inline">/ search</span></div>
    <div class="overflow-hidden rounded-xl border border-slate-800 bg-slate-900"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b border-slate-800 text-xs uppercase tracking-wider text-slate-500"><tr>@foreach($definition['columns'] as $label)<th class="px-5 py-4">{{ $label }}</th>@endforeach<th class="px-5 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-800">@forelse($records as $record)<tr class="transition hover:bg-slate-800/50">@foreach(array_keys($definition['columns']) as $column)<td class="px-5 py-4 text-slate-300">@php($value = data_get($record, $column)) @if($column === 'read_at')<span class="rounded-full px-2.5 py-1 text-xs {{ $value ? 'bg-slate-800 text-slate-500' : 'bg-blue-500/10 text-blue-300' }}">{{ $value ? 'Lu' : 'Non lu' }}</span>@elseif(is_bool($value))<span class="rounded-full px-2.5 py-1 text-xs {{ $value ? 'bg-emerald-500/10 text-emerald-300' : 'bg-slate-800 text-slate-500' }}">{{ $value ? 'Oui' : 'Non' }}</span>@elseif($value instanceof \BackedEnum){{ $value->value }}@else{{ Str::limit((string) ($value ?? '—'), 70) }}@endif</td>@endforeach<td class="px-5 py-4 text-right whitespace-nowrap">@if($resource === 'messages' && ! $record->read_at)<button wire:click="markRead({{ $record->id }})" class="text-blue-400 hover:text-blue-300">Marquer lu</button>@endif<button wire:click="delete({{ $record->id }})" wire:confirm="Supprimer cet élément ?" class="ml-4 text-red-400 hover:text-red-300">Supprimer</button></td></tr>@empty<tr><td colspan="{{ count($definition['columns']) + 1 }}" class="px-5 py-12 text-center text-slate-400">Aucun élément trouvé.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-800 px-5 py-4">{{ $records->links() }}</div></div>
</div>
